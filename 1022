#include <stdio.h>
int main()
{
	int a,b,d,sum,i=0,j;
	int sum[50];
	scanf("%d%d%d",&a,&b,&d);
	sum=a+b;
	if(sum==0)
	{
		printf("0");
		return 0;
	}
	while(sum!=0)
	{
		num[i++]=sum%d;
		sum=sum/d;
	}
	for(j=i-1;j>=0;j--)
	{
		printf("%d",num[j]);
	}
	return 0;
}
